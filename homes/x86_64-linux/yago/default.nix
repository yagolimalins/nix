#
# homes/x86_64-linux/yago — Home Manager base
#
# The username/home directory are inferred by Snowfall Lib from this
# directory's name. Shared modules and XDG basics live in homes/common.nix
# (wired via homes.modules in flake.nix). Put user-specific overrides here.
#
{ ... }:

{
}
