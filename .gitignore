# nix build
result
result-*

# direnv
.direnv/

# secrets / local overrides
.env
.env.*
*.pem
*.key

# editor / OS junk
.DS_Store
*~
*.swp
.idea/
.vscode/
# Contents, not the dir — git cannot re-include inside an excluded directory.
.cursor/*
!.cursor/rules/
